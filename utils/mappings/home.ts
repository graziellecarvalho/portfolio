import { HomeLayoutProps } from 'layouts/home/HomeLayout.types';

export function formatHomePageContent(data) {
	const content: HomeLayoutProps = {
		text: 'Texto da página inicial',
	};

	return content;
}
