import { ImageProps } from '@/typings/ImageProps';

export interface ArticleCardProps {
	badge: string;
	title: string;
	excerpt: string;
	buttonText: string;
	buttonUrl: string;
	image: ImageProps;
}
